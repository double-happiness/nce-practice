# Change：情景对话二级目录 + 使用说明同步

**日期**：2026-07-04

## 需求

情景对话场景过多（73 类），单层侧栏不便浏览；需在现有场景分类上增加 **大类 → 场景** 二级目录，并同步更新使用说明。

## 实现

### 数据与 API

- `lib/dialogue-meta.js`：新增 `GROUPS`（8 大类）、`CATEGORY_GROUP` 映射、`buildGroupTree()`
- `GET /api/dialogue/meta` 响应增加 `groups` 字段

### 前端

- `public/js/feat-dialogue.js`：侧栏折叠式二级目录；搜索支持大类/场景名；`meta.groups` 缺失时前端兜底重建

### 8 个大类

居家社交 · 餐饮购物 · 出行旅游 · 职场学习 · 住房物业 · 健康运动 · 生活服务 · 休闲文娱

## 文档同步

- `docs/用户指南.md` §5.3 情景对话二级目录
- `public/help.html` §6 情景对话怎么用
- `README.md` 功能表与内容覆盖
- 首页欢迎引导补充「原声课文 / 情景对话」
